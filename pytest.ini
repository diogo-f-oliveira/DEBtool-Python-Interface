[pytest]
markers =
    integration: mark a test as an integration test that may be slower or require external resources
    slow: mark a test as slow to run
    matlab: mark a test as requiring a MATLAB session/tooling
addopts = -ra
testpaths = tests
